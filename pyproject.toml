[tool.poetry]
name = "mc-converter"
version = "1.0.3.5"
description = ""
authors = ["RozeFound"]
license = "MIT"
readme = "README.md"
repository = "https://github.com/RozeFound/modpack-converter"
homepage = "https://github.com/RozeFound/modpack-converter"

keywords = [
    "minecraft",
    "mods",
    "modpack",
    "converter"
]

[tool.poetry.dependencies]
python = "^3.10"
murmurhash2 = "^0.2.9"
aiohttp = "^3.7.4"
tomli = "^1.2.1"
pytoml = "^0.1.21"
tenacity = "^8.0.1"

[tool.poetry.dev-dependencies]
bandit = "^1.7.0"
flake8 = "^4.0.1"

[tool.poetry.scripts]
mc-converter = "mc_converter:main"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
